# App Integration Tests

Use this directory for tests that cross an application boundary without
requiring the deployed stack, such as Better Auth persistence, app API routes,
Cloudflare adapters, and SQLite/Neon repository contracts.
