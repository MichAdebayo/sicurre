# API Contracts

Sicurre is API-first. The OpenAPI spec is the contract between:
- Dashboard ↔ Sicurre API
- Gmail listener ↔ Classifier API

All code must conform to `openapi.yaml`. If behavior changes, update the spec first.
