# Deploy

This folder is reserved for host/server provisioning and runtime configuration,
such as reverse proxy and observability files.

Expected subfolders:

- `nginx/`
- `alloy/`

Application container build files live at the repository root.
